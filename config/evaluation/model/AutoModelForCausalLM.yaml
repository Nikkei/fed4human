transformers:
  _target_: transformers.AutoModelForCausalLM.from_pretrained
  pretrained_model_name_or_path: ${pretrained_model_name_or_path}
  dtype: ${torch_dtype}
  device_map: auto
  attn_implementation: ${get_attn_implementation:${pretrained_model_name_or_path}}
